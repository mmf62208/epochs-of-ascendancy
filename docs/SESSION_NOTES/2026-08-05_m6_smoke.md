# M6 / full-test smoke — human play notes

**Date:** 2026-08-05  
**Scenario:** `world_accurate` (~3520)  
**Godot:** 4.7.1 via `tools/run_godot.sh`  
**Plan:** Ladder A — human proof (§0b + war loop + 20d path)  
**Player:** mikef · co-pilot: Grok  

**2026-08-12 director pointer:** B hang (item 7 first row) was fixed 2026-08-07 (single-pass collect + cache + no inspector). Execute hang-class (deferred inspector / full-fill / full-pin storm) is addressed in this DAG’s PR 2 (`30910c2`; hang-class greps green). §0b items 3–15 still await **human** marks. This note is not an M6 complete and is not a human replay of the duplicate item 7 checklist row.

---

## §0b checklist (mark ✓ / ✗ + note)

| # | What | Expect | Pass? | Notes |
|---|------|--------|-------|-------|
| 1 | Map loads without SCRIPT ERROR; political world | ~3520 polys, Europe focus or Home | ✓ | Full GIS world, looks good (2026-08-07) |
| 2 | Home = Europe · End = Asia · Shift+Home = full world | No void-hex stuck camera | ✓ | Home/End/Shift+Home all work |
| pan/zoom | WASD / edge / wheel | Works | ✓ | User confirmed |
| 7 | Toolbar Fronts or **B** | Toast + enemy border target | ✗ hang | **B froze game** — fixed: single-pass collect + no info panel on B + cache (2026-08-07) |
| CC pan | Menu open → no map pan | Should not pan | feedback | Fixed: modal_blocks_map_nav freezes WASD/edge/drag |
| 3 | Click 8 majors capitals (GER/FRA/ENG/USA/SOV/ITA/JAP/POL) | Names + ownership correct | ✓ | 2026-08-17 human |
| 4 | F1 political · F2 strain · F3 vitality · F4 development | Visible gradients | ✓ | 2026-08-17 human |
| 5 | F9 resources · Shift+F9 states · Ctrl+F9 terrain | Fills change | ✓ | 2026-08-17 human |
| 6 | States mode + operational zoom | State names in Europe (Alsace/Baden/Rhineland class) | ✓ | 2026-08-17 human |
| 7 | Toolbar Fronts or **B** | Toast + enemy border target; cycle with B | ✓ | 2026-08-17 human · no freeze |
| 8 | Toolbar WarLoop or **Shift+I** | Flow ON + fronts + war-path toast | ✗ hang then toast-only | 2026-08-17: Shift+I froze (sync then deferred 3520 flow). Now toast-only; no overlay. |
| 9 | **I** alone | Toggles EquipmentFlow glyphs | skip | Skip until cheap — overlay hang-class. |
| 10 | Select front province · **G** | Supply corridor capital→front polyline | ✗ hang | 2026-08-17: G froze (preview_player_route + 3520 BFS). Now toast-only: click chip → march / Ctrl+click assault. |
| 11 | Friendly province w/ formation · Ctrl+click enemy adj or Attack | Preview / assault attempt | | |
| 12 | Corridor / supply mode | Readable logistics story | | |
| 13 | Ctrl+S quicksave · mutate · Ctrl+L quickload | State survives | | |
| 14 | Advance ~5–10 days | No freeze; toasts readable | | |
| 15 | Optional: OrderCommandPanel assault/flow | Not required if map path works | | |

---

## Session summary (fill as we go)

```
In-game days advanced:
§0b fails (#):
War loop story (produce→fight) visible without F10? Y/N
Save/load OK? Y/N
Worst UX:
Best moment:
Balance (too free / harsh / invisible):
Next 1–3 engineering jobs only:
```

---

## Live feedback log

*(Grok will append as you report)*

### 2026-09-16 Maginot combat loop (human + headless)
- **Loop that landed:** pick GER chip → **Not at war with FRA** (stacked Declare war / Negotiate / Cancel) → Declare war → red attack arrow + Fight card (Likely/Tight/Bad, staff line, fogged enemy, Maginot fortified) → 1× until FRA breaks → hex **stays FRA** → auto occupy walk-in (red arrow, no Fight box) → hex GER. Empty enemy hex = occupy only. Gold line = friendly march.
- **Human ✓:** hover name-first; select + toast; Declare war then fight; FRA retreats; auto occupy; hex waits for walk-in; rear GER march gold line.
- **Hangs fixed (playtest):** inspector-on-Alsace; hover tooltip rebuild every frame (~20GB); AI empty-hex occupation chain (~25GB); 8-nation `day_ai` ~1.4s/day + 2MB calendar autosave on F5; stacked right-clicks; `FormationMovement.has_method` grey map.
- **Still honest:** F5 1× for many days can still hitch; don't spam clicks; M6 20d/60d notes open; `--quick` living-loop product still has pre-existing extra greps.
- Headless: `will_gate` → `declare_war` → `start_battle` PASS on Maginot `710173→710739`.

### 2026-09-14 click-order loop (machine — next human §0b 11–15)
- Selected GER chip: **right-click** own land marches; **right-click / Ctrl+click** adjacent enemy opens the fight sheet for **that** pair. **Start battle** still calls `start_land_battle` (never execute on the click). Right-drag (≥8px) still pans.
- Play-strip **Assault** → `open_living_assault` (living sheet, not dual `apply_assault`). Unit card **Open fight** unchanged.
- Second GER land parks on the Maginot neighbor (Rastatt `710176` class) and is on the living chip allow-list so F5 can march it onto Maginot / reinforce.
- Open fight no longer teleports the selected GER division onto `710173`.
- Next human: F5 GER 1936 · click Maginot chip · right-click Alsace · Start battle · unpause. Then click rear chip · click Maginot to march.

### 2026-09-11 collaborative F5
- Launched `tools/run_godot.sh res://scenes/TestScenario.tscn` · Godot 4.7.1 · pid 149172
- Boot: `provinces_world_accurate` **3520** · 154 leaders · 253 unit icons · GER chip `710173` · JAP `903981` · fleet `950001` · Europe frame · sim **paused** · SCRIPT ERROR 0
- Goal this session: remaining §0b **11–15** + Maginot living loop (pick → march → Ctrl+click fight). G/Shift+I hang-safe toast-only — re-check honestly.
- **Play blocker:** FRA political fill S/E of LUX hovered as Luxembourg. Cause: capital-star snap disk (min 56 wu, pre-empt `prefer_capital(..., -1)`) larger than LUX `710977` (~17×21). Fix: same-owner snap only + hex-first hover; disk floor 12 wu. Reload scripts / restart F5 to pick up.
- **Play follow-up:** same steal into GER NE of LUX. Nearest-centroid pick (grid hashed by centroid only) lets tiny LUX win over Moselle/Bitburg-Prüm whose centroids sit far from the border. Fix: AABB cells in MapPickGrid + exact PIP only (no centroid fallback). F5 relaunched pid 151617.
- **Play:** old round pins (cyan outline, pink glyph) still on GER — retrowave 32 plates (fighter/infantry/fort) used as map counters next to living I/A/H chips. Fix: map counters remap retrowave → NATO; skip fort/port/airfield furniture. F5 pid 153278.
- **Human ✓ LUX border:** FRA south/east and GER NE of LUX hover as FRA/GER, not Luxembourg (2026-09-11).
- **Play:** cyan/pink pins still visible after NATO remap — leftover retrowave sprites (recon/aa/air/fort) still loaded. Hard-strip: never add a retrowave texture to DemoUnitIcon. F5 pid 154852.
- **Play screenshot:** GER interior carpet of cyan/pink pins at Europe zoom = 200+ interior garrisons as map chips. Cull: operational zoom shows border/capital/air/fleet/Maginot only; tactical zoom shows all. Strip retrowave by resource_path. F5 pid 156262.
- **Play:** pins still there after cull. Nuclear: no Sprite2D on DemoUnitIcon; do not create land-garrison chips (Maginot + capital + air + fleet only); strip retrowave sprites under province nodes. F5 pid 157149.
- **Play:** black screen + lost NATO images inside divisions. Reverted Maginot-only cull and restored NATO sprites on living plates; still skip retrowave textures and tag-only pins. F5 pid 157852 · 3520 board. Cyan/pink pins if still present are a separate overlay.
- **Play:** pins still GER+AUS. Cause: ~250 TYPE_DIVISION chips (one per hex) plus always-on special_site draw. Fix: land chips only Maginot+major pids; skip garrisons; special_sites only if Sites layer on. Icons placed **56**. F5 pid 159283. NATO glyphs kept.
- **Play:** fewer units but pins remained. Sprite inventory: **zero retrowave unit sprites**. Pins were **F9 resource glyphs** (`coal_24.png` = cyan outline + pink diamond) drawn on political. Gated to resources mapmode only. F5 pid 160315.
- **Play:** pins still after first F9 gate (fail-open when overlay could not see MapRenderer). `show_resource_icons` default **false**; only `set_map_mode("resources")` turns it on. F5 pid 160691.
- **Human ✓ political pins gone:** coal/steel resource glyphs no longer on political (2026-09-11). F9 still the resources mapmode.
- **Director:** resource glyphs never on political. F9 resources always. Other mapmodes only at tactical zoom. coal_24 did not read as coal (looked like a unit pin) — icon art later.
- **Play:** Maginot chips visible but unclickable; province double-click no inspector. Causes: title boot ate hex clicks while node existed; camera-hold swallowed inspector; chip hit disk too small. Fix: boot only if visible; inspector despite camera hold; hit disk 80/40. Political hex → province inspector (not a country window). F5 pid 162601.
- **Play:** Switzerland / Liechtenstein names overlap. Nudge LIE east of SWI; AABB collision keeps larger country still. F5 pid 163305.
- **Play:** still no unit/province select. After left-pan, `_left_ready_for_still_click=false` until Home/wheel; camera-hold after zoom ate clicks. Fix: pan no longer disarms still-click; camera-hold does not block pick. Title boot toasts Begin. F5 pid 164268.
- **Play:** clicks still dead. `_input` left-press called `_mark_left_pan_blocked_pick` + handled so `_unhandled_input` never picked. Stop eating left-click in `_input`. LIE nudge is AABB-step from own centroid (not SWI+210). F5 pid 165132. `[pick]` logs on still-click.
- **Play hang:** not a freeze — 3 Maginot unit picks + 3 inspector Closes, then Close armed `_close_click_guard` + pan-skip so later clicks never reached pick. Dismiss no longer sets skip/block; close-guard does not eat the next still-click. F5 pid 166144.
- **Play:** state double-click + division card work; Close on unit card dead; want drag/resize. Unit card is DraggablePanel, title-bar drag, cyan resize grip, Close via gui_input. LIE closer (human). F5 pid 167154.
- **Human ✓ unit card Close** works (2026-09-11).
- **Play:** scrollbar on unit card also panned the map. Press over UnitDetailPopup no longer arms left-pan; process cancels pan while cursor is on the card. F5 pid 167796.
- **Play:** same for province inspector. Rect-hit `_mouse_over_inspector_chrome` covers InfoPanel + unit card + fight sheet (scrollbars included). F5 pid 168359.
- **Play:** black sharpie marks near LUX/NLD/SWI. 20-vert NUTS international edges drawn thick black. Skip segments <4 wu; thinner frontiers; lighter nation outline; province names only at zoom≥2.05. F5 pid 168999.
- **Play:** clicking another unit stacked a second card (`queue_free` delayed). Immediate `_clear_unit_detail_popup` replace; keeps last size/pos. F5 pid 169603.
- **Human ✓ unit box replace** (2026-09-11).
- **Play screenshot:** isolated black dashes on NLD/LUX interiors = coarse 2.5 edge-hash merging unrelated NUTS edges as "international". Precision 1.0 + midpoint reject if >6 wu. F5 pid 171334.
- **Play:** London star selected ENG chip to the right; LIE/SWI/HUN stars looked off. Star pick first (glyph disk); stars at bbox center with font-px offset (not Label min-size). Star click → capital province inspector. F5 pid 173496.
- **Play:** still cannot click star without division. Star hit used Label.global_position (wrong space). Now `_capital_star_world_center` via `to_global`; skip unit if click nearer star than chip. F5 pid 174204.
- **Play:** London star selected Hertfordshire (711405). Capital is 711414 Camden and City of London (tiny NUTS). Neighbor hex won. Tiny-capital disk ≥24 wu; same-owner star snap on hex pick. F5 pid 175396.
- **Human ✓ capital stars** better placed (London/Paris centered on hex). Click star → capital province. F5 pid 176518.

### Blockers / bugs
- **P0 FIXED (pre-play):** `TestRunner.gd` parse error — `DebugOverlay.has_method()` invalid on class_name under Godot 4.7.1 → TestRunner failed to load → board fell back to phase1 (~471) instead of world_accurate. Fixed by calling static `toast_map_debug` directly (same pattern in OrderCommandPanel). Relaunch confirmed: **3520 provinces**, `provinces_world_accurate`, SCRIPT ERROR gone.
- **P0 B hang (3x human):** Root cause remaining path: `highlight_supply_route_path` toggled full supply overlay. Stripped B to toast+select only. Collect single-pass earlier.
- **CC pan under menu:** MainMenu is CanvasLayer (not CanvasItem) — `modal_blocks_map_nav` never saw it. Fixed name/CanvasLayer detection.
- **Capital stars:** city layer had capital flags but `special_features.capital` not stamped → no map stars. Stamp on load + gold ★ glyph.
- **F9 resources:** empty land near-black looked “missing”; improved empty blend + stronger resource hues.
- **F4 development:** low-dev floor too dark — raised floor so land stays visible.
- **Shift+F9 states:** unassigned state_id was near-black — now muted political grey.
- Watch: log said `Leaders loaded … active ~0` after historical roster load — verify leaders visible when clicking capitals / Leader UI.
- **Sea geometry fix (2026-08-07):** 172 sea/strait zones reanchored by name→lon/lat (Danish Straits was over Spain/Med). Backup `.bak_pre_sea_reanchor`.
- **Capitals all tags:** city-name table stamps NLD/BEL/SWI/LUX/DNK/… not only 8 majors.
- **F3/F4 contrast:** stronger teal→lime vitality and brown→gold development.
- **Choke tooltip:** hover/inspector notes naval chokepoint.
- **Deferred:** state-as-clickable-unit borders · per-province resource icons · land/sea border emphasis mapmode.

### Machine F5 boot probe (2026-08-22) — **not** a human §0b session
- `EOA_UNIT_ORDER_QA=1 tools/run_godot.sh --headless --path . res://scenes/TestScenario.tscn --quit-after 120` loaded `provinces_world_accurate` **3520** (ScenarioLoader forced exact count).
- UNIT ORDER QA line never printed before `--quit-after` (tech-unlock warning storm on load).
- Maginot `-s` harness: living F5 unpause **+5 days flushed=15** (`day_emit`/`day_ai`/`day_battles`) **RESULT=PASS**.
- Follow-up: `module_unlock` handled in `TechnologyUnlockRegistry` (0 unhandled warnings). UNIT ORDER QA **RESULT=PASS** (GER 710173 chip + march + assault) after skipping headless evidence/4mo cycles.
- §0b items **11–15 Pass? still blank** — human must mark those. This note is not M6 complete.
- **2026-08-23 playtest:** gold star for London (and DC / Roma) was hard to click — City of London bbox ~3 world units vs ~28px star. Zoom-aware capital snap + star-over-chip on plain click. Not a human §0b complete.

### UX confusion
- F2 purpose unclear (explain: stress heatmap, not political). Unit diamonds = chokepoints/OOB pins.
- Scotland/Iberia visual anomalies (underlay vs GIS / fill) — data owns ENG/SPA correctly; investigate fill/underlay if re-reported.
- Capital stars missing outside Europe view: LOD hid glyphs + whitewashed gold — fix always-visible gold capitals.
- B only toast: add light camera focus to target (no supply overlay).


### Positive moments
- Machine load path: world_accurate 3520 + MapPickGrid + Europe focus + PAUSED start — green for human session.

### Balance notes
- 

### Engineering priority (top 3)
1. Complete §0b with human (in progress)
2. 
3. 

### Pre-session machine status
- Grok Build: **0.2.118 → 1.0.0** installed (`grok update`). Restart Grok TUI when convenient to run on 1.0.0 binary.
- EOA launched: Godot 4.7.1 · TestScenario · board ready for play.
