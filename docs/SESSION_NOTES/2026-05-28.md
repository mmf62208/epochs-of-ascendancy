# Session Notes — May 28, 2026

## Map Generation Pipeline

### Accomplished

- Stood up `tools/map_generation/` with shared `lib/` modules and Europe Phase 1 config.
- **Naval analysis:** coastal detection, chokepoint/bridge identification, naval importance scoring (`naval_analysis.py`).
- **Subdivision:** PCA-based province splitting with coastal preservation on cut edges (`subdivision_utils.py`).
- **Merge tool:** `apply_phase1_merge.py` — child ID assignment, adjacency rebuild with chokepoint protection, layered attribute merge (terrain, resources, economy, states, strategic regions).
- **Test scenario:** ~180-province persistent map in `data/provinces_phase1_test/` + `phase1_europe_test.json` scenario.
- **Godot validation:** Debug Overlay button to load the test scenario; camera positioning and diagnostics for quick visual review.

### Next session

- Refine splitter so coastal segments stay clean along cut lines.
- Export production-ready layered JSON (not only test bundles under `tools/map_generation/output/`).
- Increase subdivision density toward the 350–450 province Europe Phase 1 goal.

## Hidden Hand Design

- Authored [HIDDEN_HAND_DESIGN.md](../HIDDEN_HAND_DESIGN.md): overview, influence/alignment mechanics, discovery progression, player resist/join paths, internal rivalry between London / Vatican / Washington, and late-game “play as a power center” mode.

## Other (same period)

- Infrastructure development + special sites groundwork committed separately; compile-error fixes across map/trade/supply/UI scripts.
